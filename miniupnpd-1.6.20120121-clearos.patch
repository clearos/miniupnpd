Only in miniupnpd-1.6.20120121-clearos: config.h
diff -rup miniupnpd-1.6.20120121/genconfig.sh miniupnpd-1.6.20120121-clearos/genconfig.sh
--- miniupnpd-1.6.20120121/genconfig.sh	2012-01-20 16:58:33.000000000 -0500
+++ miniupnpd-1.6.20120121-clearos/genconfig.sh	2013-09-09 09:58:53.631294852 -0400
@@ -184,6 +184,12 @@ case $OS_NAME in
 			OS_VERSION=`cat /etc/gentoo-release`
 			OS_URL=http://www.gentoo.org/
 		fi
+		# ClearOS special case
+		if [ -f /etc/clearos-release ]; then
+			OS_NAME=ClearOS
+			OS_VERSION=`grep ^base_version /etc/product | awk '{ print $3 }'`
+			OS_URL=http://www.clearfoundation.com/
+		fi
 		# use lsb_release (Linux Standard Base) when available
 		LSB_RELEASE=`which lsb_release`
 		if [ 0 -eq $? ]; then
